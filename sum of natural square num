#include <stdio.h>
int main()
 {
    int i, n;
    printf("Enter a positive integer: ");
    scanf("%d", &n);
    printf("Natural square numbers up to %d:\n", n);
    for ( i = 1; i*i <= n; i += 1) 
	{
        printf("%d ", i*i);
    }

    printf("\n");
    return 0;
}
